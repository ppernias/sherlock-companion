# Sherlock Companion - Environment Variables
# Copy this file to .env and customize the values

# Security
JWT_SECRET=your-secret-key-change-in-production

# Game PIN (used by players to access the game)
GAME_PIN=1895

# Default admin credentials (only used on first initialization)
ADMIN_EMAIL=admin@sherlock.local
ADMIN_PASSWORD=holmes221b

# OpenAI API Key (required for AI prompt and image generation)
OPENAI_API_KEY=sk-your-openai-api-key

# Frontend port (Docker only)
FRONTEND_PORT=3000
